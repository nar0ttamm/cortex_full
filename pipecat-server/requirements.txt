# CortexFlow Pipecat Server
# Install: pip install -r requirements.txt

# Pipecat with telephony + AI service extras
pipecat-ai[deepgram,elevenlabs,openai,silero]==0.0.108

# Web server
fastapi[standard]>=0.115.0
uvicorn[standard]>=0.30.0

# HTTP clients
aiohttp>=3.9.0
httpx>=0.27.0

# OpenAI SDK (for summarize + translate)
openai>=1.30.0

# Utils
python-dotenv>=1.0.0
loguru>=0.7.0

# Telnyx webhook Ed25519 signature verification
PyNaCl>=1.5.0
